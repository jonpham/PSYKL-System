worker_processes auto;
events { worker_connections 1024; }

http {
  include /etc/nginx/mime.types;
  default_type application/octet-stream;

  sendfile on;
  keepalive_timeout 65;
  server_tokens off;

  gzip on;
  gzip_types text/plain text/css application/javascript application/json image/svg+xml;
  gzip_min_length 1024;

  server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: any unmatched route serves index.html so client-side
    # React Router (M2+) handles deep links.
    location / {
      try_files $uri $uri/ /index.html;
    }

    # Build manifest read by the in-app updater — must never be cached, or the
    # app cannot tell that a newer bundle has been deployed.
    location = /version.json {
      add_header Cache-Control "no-store";
    }

    # The service worker itself — revalidate so a new deploy is picked up.
    location = /sw.js {
      add_header Cache-Control "no-cache";
    }

    # Hashed static assets — long cache.
    location ~* \.(js|css|woff2?|png|jpg|svg|ico)$ {
      expires 1y;
      add_header Cache-Control "public, immutable";
    }
  }
}
