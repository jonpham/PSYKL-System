# Design: Todo Experience — Apple Reminders-grade task management on the PWA

Generated by `/office-hours` (gstack) on 2026-08-12
Reviewed by Codex following the `/office-hours` Spec Review Loop contract on 2026-08-12
Branch: `feat/plan-psykl-loop`
Repo: [jonpham/PSYKL-System](https://github.com/jonpham/PSYKL-System)
Status: APPROVED
Mode: Builder
Milestone tag: `todo-experience`

> **Glossary** (each doc carries its own, per [`AGENTS.md`](../../../AGENTS.md) → Design Doc Discipline):
>
> - PWA = Progressive Web App.
> - PSYKL = a self-defined period of focused work, user-tunable, defaulting to 25 minutes.
> - CRUD = Create, Read, Update, Delete.
> - LWW = Last-Write-Wins — M2's conflict-resolution rule: on conflict, the most recent write wins.
> - IndexedDB = the browser's local database; PSYKL's PWA treats it as the source of truth and syncs it to the server.
> - TDD = Test-Driven Development.
> - UX = User Experience.
> - UI = User Interface.

## Re-scope, 2026-08-14

Recorded during `/plan-design-review` after an interactive prototype of the proposed visual system was built and used.

**What changed:** the design _philosophy_. The 2026-08-13 `/design-consultation` produced a bespoke industrial visual identity ("the Ledger") and a governing thesis ("the list is arranged, not scored") that derived the product from first principles. Prototyped against 44 real tasks, it read as rigid and engineering-focused. More expensively, its philosophy was absorbing every planning pass — each review relitigated the identity instead of the product.

The initiative is now **clinical craft parity with Apple Reminders, Things 3, and TickTick**, on a conventional and themable UI. Those three apps are the UI/UX bar, not a feature list.

**What did not change:**

- **The Feature Inventory below is unchanged**, except for the addition of theming. Notifications, repeat rules, priority flags, and shared lists stay out. Subtasks stay deferred.
- **Premises P1 through P5 all stand**, including P2's refusal of notifications and clock-time scheduling.
- **Approach B, structure first**, and its sequence.
- **Every architectural risk and its decision** — the task-shaped sync queue, fractional positions over integer indexes, and the unresolved section representation.

**What was retired:** the bespoke visual identity is demoted to a selectable theme, and the anti-convention stance that came with it (no color on chrome, no overdue styling, no platform font stack, no rounded corners) is reversed. See [`docs/DESIGN.md`](../../DESIGN.md) → Decisions Log.

## Offline Posture, 2026-08-18

Decided during `/plan-eng-review`. **Offline is a degraded mode, not a first-class one.** This is a deliberate reversal of the M2 positioning; `docs/PRODUCT.md` → Engineering Constraints was updated in the same pass.

| Rule                 | Behavior                                                                                              |
| -------------------- | ----------------------------------------------------------------------------------------------------- |
| Offline reads/writes | Continue to work.                                                                                     |
| Nag threshold        | At **25** unsynced changes, a persistent banner tells the user to reconnect.                          |
| Hard ceiling         | At **100** unsynced changes, new writes are refused until the device reconnects and the queue drains. |
| Deletes              | Never destructive on the client. A delete is a **move to a `Recently Deleted` list**.                 |
| Purge                | The **server** hard-deletes anything in `Recently Deleted` untouched for **30 days**.                 |

### Why this replaces the cascade-delete design

Deleting a list no longer enqueues one operation per task. It enqueues **one** move. Tasks stay attached to their list; the list itself moves to `Recently Deleted`. This removes the 201-operation burst that the earlier cascade decision created, makes every delete recoverable, and matches Apple Reminders.

### Data-model decisions locked here

| Question                 | Decision                                                                                                                                                                                                                                                                                                  |
| ------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Cross-entity references  | **No foreign keys.** `task.list_id` and `task.section_id` are nullable text. The server accepts a reference it has not seen yet; an orphan sweep heals strays into the default list. The client mints all IDs (`uuidv7`), so a reference is always correct once both rows land.                           |
| Section representation   | **A real `sections` table** (`id`, `user_id`, `list_id`, `title`, `position`), same no-FK pattern. Resolves Risk 3. Rename and reorder are single-row writes that never collide under Last-Write-Wins.                                                                                                    |
| Ordering representation  | **`fractional-indexing` npm package** (the Figma/rocicorp algorithm), keys generated client-side, stored as Postgres `text COLLATE "C"` so byte order matches JavaScript string comparison. Resolves Open Question 5.                                                                                     |
| Sync-queue upgrade path  | **Migrate, never drain.** Draining needs network; a user upgrading while offline would lose queued writes, which Spec 1's own user story forbids. The v1→v2 IndexedDB upgrade rewrites `sync_queue` and `failed_ops` in place, mapping `task_id` → `entity_type` + `entity_id`. Resolves Open Question 2. |
| Replay ordering          | **Order-independent.** Replay no longer stops at the first transient failure, and an entry moves to `failed_ops` after an attempt ceiling. Supersedes ADR-M2-012.                                                                                                                                         |
| Device-local preferences | Theme choice and section collapse state live in the existing **`sync_meta` IndexedDB store**, which is never enqueued and therefore never syncs. No new store needed.                                                                                                                                     |

## Problem Statement

M2 shipped a technically excellent offline-first todo list — IndexedDB source of truth, sync queue, background sync, multi-device end-to-end coverage — wrapped around a **rudimentary** task experience. The PWA has four UI components (`TaskCreateForm`, `TaskList`, `Toast`, `VersionFooter`) and a flat, single, unordered list of title-only tasks.

The operator's words: _"the ToDo use cases and UX is very rudimentary and I want to replicate/clone an Apple Reminders like experience before adding new use cases such as PSYKL loops and daily task planning."_

So the gap is not sync, and not the differentiator. It is that the app is not yet a good place to keep tasks, which makes it a poor foundation for everything after.

## What Makes This Cool

Not the feature list — Apple Reminders already exists. Two things:

1. **It's the substrate for the differentiator.** PSYKL sessions attach to tasks. A task model that only has a title can't support "tasks of type X land in low-energy windows"; a task model with lists and tags can. Every structural feature here is also a future retrospective dimension.
2. **Offline-first makes the boring features interesting again.** Manual drag-to-reorder is a solved problem in a server-backed app and a genuinely hard one in an offline, multi-device app. Getting it right — see the fractional-index decision below — is the kind of problem that only shows up because M2 was built properly.

## Constraints

- **Single-user, multi-device, siloed.** No collaboration, ever (`docs/PRODUCT.md` → Sync and Sharing Model). No shared lists, no assignees, no comments — a large fraction of Apple Reminders is therefore out of scope by definition.
- **PWA is the exploration client.** No native client work in this milestone.
- **Everything must survive offline.** Any new entity has to round-trip through the sync queue and reconcile under LWW.
- **The five-layer test pyramid holds** (`AGENTS.md` → Test Discipline). TDD ordering is mandatory; tests ship in the same pull request as the behavior.
- **Deploy target is unchanged** — robin/k3s via ArgoCD, semver image pins.

## Premises

Agreed during the session. P2 was corrected by the operator; the correction is recorded rather than the original.

**P1 — This milestone is todo experience, not the PSYKL loop.** No timers, cycles, or breaks are built here. The `psykl-loop` milestone follows this one.

**P2 — Dates are facts about a task, never a plan for your attention.** A task may carry a due date, and a time when the deadline genuinely has one (a form closes at 5pm). The app never sends a notification and never asks what time you intend to work on something. Recurrence is **not** Apple's repeat rules; the eventual mechanism is **template tasks that spawn instances**, deferred to a later milestone, but the model must not preclude it.

> This is a refinement of `docs/PRODUCT.md`'s anti-clock stance, not a reversal. The product rejects clock-time scheduling of future work. A deadline is a constraint on the task; a schedule is a claim on your attention. Reminders conflates them. PSYKL does not.

**P3 — One feature per pull request, validated by real use before the next starts.** Each feature merges, deploys to robin, and gets lived with. If use says the feature is wrong, it changes or comes out before anything stacks on top of it. Subtasks specifically are **not built** until lists and sections have been used and the operator can say whether nesting is still wanted.

**P4 — Structural features cost far more than they look.** Notes, optional due dates, and interaction polish are one column or zero columns. Lists, sections, and ordering change the Postgres schema, the IndexedDB store, and the sync queue simultaneously.

**P5 — Do not make PSYKL sessions harder to attach later.** A session record points at a task. Keep task identity stable and singular. Low bar, stated so it doesn't get violated by accident.

## Feature Inventory

Fine-grained rather than bucketed, at the operator's request — the four coarse groups were rejected as too blunt to commit to.

| Feature                              | Cost surface               | Status                              |
| ------------------------------------ | -------------------------- | ----------------------------------- |
| Lists                                | new table + `task.list_id` | In scope — first structural work    |
| Sections within a list               | depends on lists           | In scope                            |
| Manual drag-to-reorder               | ordering value + sync      | In scope — see risk below           |
| Invisible ordering rank              | ordering implementation    | In scope — not visible to users     |
| Notes body                           | one column                 | In scope                            |
| Optional due date (+ optional time)  | one or two columns         | In scope, per P2                    |
| Tags                                 | new table + join           | In scope, late — feeds future retro |
| Swipe to complete / delete           | UI only                    | In scope                            |
| Row density + animation              | UI only                    | In scope                            |
| Native-feeling inline edit           | UI only                    | Partly shipped in M2 Spec 5         |
| Search                               | query + UI                 | In scope, late                      |
| Theming (2 themes, device-local)     | token layer + preference   | In scope — added 2026-08-14         |
| Priority / flag as a visible concept | —                          | **Out** — replace with ordering     |
| Subtasks (`parent_id`)               | recursion everywhere       | **Deferred** — validate lists first |
| Notifications / alerts               | —                          | **Out** — P2                        |
| Repeat rules                         | —                          | **Out** — templates instead, later  |
| Shared lists, assignees              | —                          | **Out** — single-user by design     |

## Architectural Risks

### 1. The sync queue is task-shaped

`components/web_client/src/db/idb.types.ts:9` defines `SyncQueueEntry` with a literal `task_id` field; both `sync_queue` and `failed_ops` index on it, and `op` is typed `'create' | 'patch' | 'delete'`. The coupling continues through `components/web_client/src/sync/replay.ts`, which treats successful replay responses as `Task` rows and writes them back through `putTask`, and `components/web_client/src/sync/replay.transport.ts`, which routes every queued operation to `/tasks` endpoints. Lists are a second syncable entity — created, renamed, and deleted offline — so the queue must become entity-agnostic (`entity_type` + `entity_id`) before any structural feature lands.

This is the keystone of the milestone. It is also exactly what the `psykl-loop` milestone needs for its session entity, so the cost is paid once and used twice (P5, discharged rather than merely respected). The implementation must cover the full path: IndexedDB schema versioning and migration, queue and failed-operation indexes, replay dispatch by entity type, response handling per entity store, generated OpenAPI client types, and server endpoints for each new syncable entity.

### 2. Manual reorder conflicts silently under LWW

Reorder on device A and on device B while both are offline, and LWW does not merge intent — it compares row timestamps. With integer indexes, a single reorder often renumbers many tasks, so later patches from one device can overwrite earlier position patches from another and produce lost ordering intent, duplicate or sparse positions, or a surprising hybrid order. A user losing a hand-arranged list is a bad, memorable bug, and it is invisible until it happens.

**Decision: fractional positions, not integer indexes.** Inserting between `1.0` and `2.0` writes `1.5`, so a reorder touches one row instead of renumbering the list, and concurrent reorders in different regions of the list do not collide. Decided now, before the feature is built, because retrofitting it means a migration over live data.

The representation is deliberately not settled here. A LexoRank-style lexicographic fractional-index key, like Jira's rank keys, is a leading candidate because it stores sortable strings instead of decimal midpoint values and can avoid numeric precision exhaustion. `/plan-eng-review` should compare it against numeric and fixed-width decimal alternatives before specs are written.

This does **not** make ordering conflict-free. Concurrent moves of the same task, or moves that choose the same gap, still resolve by LWW. The design goal is to shrink the conflict surface from "many rows per reorder" to "usually one row per reorder," then test the remaining conflict cases explicitly.

### 3. Sections may not need to be an entity

A section could be a real row (`sections` table, `task.section_id`), a grouping value on the task, or an ordered embedded descriptor on the List model such as `list.sections = [{ id, title, order_key }]` with tasks pointing at the embedded section ID. A real row is easiest to sync independently. A task grouping value is simplest but weak for manual section ordering and rename semantics. An embedded List array avoids a second top-level entity but makes section edits conflict at the whole-list row under LWW unless the sync design gives list fields narrower merge semantics. Deferred to design and engineering review, flagged so it is decided deliberately.

## Approaches Considered

### Approach A: Cheap wins first

Ship everything that touches no schema — swipes, density, animation, inline edit — then single-column additions (notes, optional due date). Lists, sections, and ordering last.

- **Effort:** M (human: ~2-3 weeks / Claude Code: ~2-3 sessions) · **Risk:** Low
- **Pros:** Something feels better within a day; no migration; establishes the ship-one-thing rhythm cheaply.
- **Cons:** Polishes a flat single list — the most toy-like property survives longest. Row layout is reworked when sections arrive, so some polish is done twice.

### Approach B: Structure first

Generalize the sync queue, then lists → sections → ordering; then detail columns; then polish last, on a layout that has stopped moving.

- **Effort:** L (human: ~3-4 weeks / Claude Code: ~3-4 sessions) · **Risk:** Medium
- **Pros:** Closes the largest gap first. Polish lands once, on final layout. Pays for the `psykl-loop` session entity's sync needs at the same time.
- **Cons:** Slowest to a felt improvement. Front-loads the fractional-index problem. Two or three pull requests before the app feels different.

### Approach C: Slice by annoyance

Order by the moments that actually irritate the operator in daily use; each slice cuts across polish, detail, and structure together.

- **Effort:** M-L (human: ~3 weeks / Claude Code: ~3 sessions) · **Risk:** Medium-High
- **Pros:** Every increment is validated by a real annoyance. Most likely to land somewhere Apple Reminders isn't.
- **Cons:** Schema churn — one slice needs half of lists, the next needs the other half. Hardest to keep the test pyramid green across partial models.

## Recommended Approach

**Approach B — structure first.** Selected by the operator.

The reasoning runs against the usual instinct to do cheap things first. The sync queue is hardcoded to tasks, and every structural feature plus the future PSYKL session needs it generalized. Doing that once, early, makes everything afterwards additive. Polishing rows before knowing whether rows nest is work done twice.

**Sequence:**

1. Generalize the sync queue to arbitrary entities as the enabling work for **Lists**
2. **Sections**
3. **Manual ordering** (fractional positions)
4. **Notes / optional due date**
5. **Swipes / density / animation**
6. **Tags**, then **search**

Each user-visible feature is one pull request, deployed and lived with before the next starts (P3). Required enabling work may ride in the same pull request only when the feature cannot exist without it; otherwise it is split into its own non-user-visible infrastructure pull request with the same test bar.

## User Stories and UX Review Gates

This initiative is the first PSYKL milestone where UX/UI quality is the product, not decoration around the product. The planning workflow therefore adds design gates before engineering lock-in:

1. ~~**`/design-consultation` before `/plan-eng-review`.**~~ **Done 2026-08-13.** Produced the UX plan and a visual system. The visual system it proposed was subsequently retired; see Re-scope above.
2. ~~**`/plan-design-review` after the consultation artifact.**~~ **Done 2026-08-14.** Built an interactive prototype rather than reviewing on paper, which is what surfaced the re-scope. Rewrote [`UX.md`](UX.md) and [`docs/DESIGN.md`](../../DESIGN.md), closed five of seven open UX questions, and added the surfaces the previous plan had left unspecified (list chrome, section creation, loading state, keyboard reorder, reconciled-arrangement state).
3. **`/plan-eng-review` — next.** Lock the data model, sync queue generalization, ordering representation, section representation, device-local preference storage, migration strategy, and test plan against the now-explicit user stories.
4. **`superpowers:writing-plans` only after engineering review is complete.** Each Spec should carry user stories and verification expectations into its DevTasks.

Open questions in this design are answered at the first gate that owns the uncertainty. UX questions are answered in `/design-consultation` or `/plan-design-review`; data-model and sync questions are answered in `/plan-eng-review`; implementation chunking questions are answered in `superpowers:writing-plans`.

## Open Questions

1. **Sections: entity, grouping value, or embedded List descriptor?** Decide during the design/engineering review pass (Risk 3).
2. **Does the generalized sync queue migrate existing queued entries, or drain first?** A user with pending offline writes at upgrade time must not lose them.
3. ~~**What is the default list?**~~ **Answered 2026-08-14** in [`UX.md`](UX.md) § 10: named `Tasks`, renameable, not special beyond the last remaining list being undeletable.
4. **Does tag design need to anticipate the retrospective?** Tags eventually power "tasks of type X." Whether that constrains tag shape now is unresolved.
5. **What is the fractional-position representation?** `/plan-eng-review` must choose the persisted type and rebalance rule. The decision is fractional positions over integer indexes; it does not yet choose Postgres `numeric`, fixed-width decimal strings, LexoRank-style lexicographic rank strings, precision limits, or compaction behavior.
6. **Milestone activation mechanics.** This design supersedes the active-roadmap role of the `psykl-loop` milestone doc committed earlier today in `c0d8759`; that scope becomes the _next_ milestone. Both `docs/initiatives/todo-experience/` and `docs/initiatives/psykl-loop/` already exist. The close-out is not a directory rename: write `todo-experience/MILESTONE.md`, rewrite `psykl-loop/MILESTONE.md` as the next milestone using the appendix below, and refresh roadmap tables.

## Success Criteria

- The operator prefers PSYKL to Apple Reminders for their own daily task capture — the honest bar, since they are the user.
- A hand-arranged list survives a concurrent reorder on two offline devices with no lost arrangement (tested, not assumed).
- Every shipped feature has been used for real before the next one starts; at least one feature is changed or removed as a result of that use. Zero such changes means P3 is being performed rather than practiced.
- Five-layer pyramid green on every pull request; every user-visible behavior carries an end-to-end test whose title reads as a user story.

## Distribution Plan

Existing deployment pipeline covers this. Merge to `main` → `cd-publish.yml` builds images → semver tag cut per `README.md` → Release → image pin bumped in `PSYKL-GitOps/apps/psykl/values-robin.yaml` → ArgoCD rolls robin. No new distribution surface.

## Next Steps

1. **Activate the `todo-experience` milestone.** Write `docs/initiatives/todo-experience/MILESTONE.md` from this approved design. Rewrite `docs/initiatives/psykl-loop/MILESTONE.md` as the next milestone, seeded from the appendix below.
2. **Refresh the roadmap** — `docs/PRODUCT.md` and `docs/PROJECT_STATUS.md` tables, since both currently name `psykl-loop` as the active initiative.
3. **Run `/design-consultation`** to turn the Apple Reminders-grade goal into concrete user stories, UX expectations, and screen-level behavior.
4. **Run `/plan-design-review`** to review that UX plan before it hardens into engineering constraints.
5. **Run `/plan-eng-review`** to lock the sync-queue generalization, section representation, ordering scheme, migrations, and test strategy.
6. **Then `superpowers:writing-plans`** to break the sequence into Specs and DevTasks.

## Appendix: PSYKL loop specification (for the next milestone)

Captured during this session; belongs to the `psykl-loop` milestone, recorded here so it is not lost.

**A PSYKL is a fixed-period event, started when the user begins a task.** It takes over the app and shows a focused countdown.

```
task tapped ──▶ WORK (fixed period, full screen)
                 ├── "mark completed" ──▶ session ends, task done
                 ├── "pause task"      ──▶ session ends (paused)
                 └── time elapses      ──▶ BREAK (fixed period)
                                             └── elapses ──▶ "Continue?" / "Do Something Else"
```

**Data model:** `Task ──1:N──▶ PSYKL session`. A session is one start/stop event — the periods during which the user worked on that task. Pausing **ends** the session; a task accumulates many sessions over its life, as real work does.

Two consequences worth carrying forward:

- **Sessions are immutable append-only events**, not mutable rows. They need none of M2's LWW machinery — two devices appending sessions cannot conflict. This is the opposite of the assumption in the superseded milestone doc.
- **The loop is deterministic in v1.** "Take a fixed break" is a rule, not a history-informed suggestion. `docs/PRODUCT.md` promises adaptive coaching at PSYKL boundaries; that requires history which does not exist yet. Ship the mechanical loop, accumulate sessions, design coaching against real data.

**Retrospective views** — all three eventually wanted, sequenced by when they can say anything true:

| View               | Data needed before it is honest |
| ------------------ | ------------------------------- |
| Time-on-task       | one session                     |
| Today's timeline   | one day                         |
| Cross-day patterns | several weeks                   |

## What I noticed about how you think

- You said _"A Psykl-Task session ends when it is paused... Just like real work, it can be started and stopped."_ You reached for how the thing actually behaves in the world rather than for a schema. That's why the model came out simpler than the one I'd written — I had cycles as mutable rows needing conflict resolution; your framing made them immutable events that can't conflict at all.
- When I offered four feature buckets you refused them: _"think its more prudent to choose them in a more fine grained way."_ And you gave the reason — subtasks wait _"until I do hands on usage to validate whether those features work for my app."_ That is a real product instinct, not caution.
- You corrected my anti-clock premise with _"it's just a fact that some tasks must be done by a certain date."_ I had turned a product principle into a rule and applied it past where it was true. You held the principle and rejected the over-application, which is harder than doing either one alone.
- _"I'm never sure with you"_ — on whether we mean the same thing by a premise. That was the most useful thing you said all session. It caught me writing compressed labels (`structure = schema + sync`) that read as agreement while hiding whether we agreed at all.
