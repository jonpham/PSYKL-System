---
status: OPEN
created: 2026-08-12
deleted_at_spec_closeout: true
---

# Codex takeover prompt

Paste the block below into Codex, or pass the file. You are taking over all editing from a
Claude Code session that hit its usage limit mid-planning.

---

You are taking over the PSYKL-System repository from another AI agent that ran out of session
budget. You have full editing authority until told otherwise. Work in
`/Users/jp/code/PSYKL-System` on branch `feat/plan-psykl-loop`.

## Read these first, in order

1. `AGENTS.md` — the working agreement. It is binding, not advisory. Read all of it.
2. `docs/PRODUCT.md` — product brief and hard scope constraints.
3. `docs/PROJECT_STATUS.md` — live project state.
4. `docs/initiatives/todo-experience/DESIGN.md` — the document you are about to review and act on.
5. `docs/initiatives/todo-experience/HANDOFF.md` — what happened and what is deliberately unfinished.

Do NOT read anything under `~/.claude/`, `~/.agents/`, `.claude/skills/`, or `agents/`. Those are
skill definitions for a different agent system — large, bash-heavy, and installed per-machine. The
next section tells you everything you need about them, so you never have to open one.

## The skill ecosystems that produced these artifacts

PSYKL-System's planning is driven by two Claude Code skill suites. **You cannot run them.** You do
not need to — what matters is the document contracts they produce, and those live in the repo. This
section exists so your output stays consistent with what came before instead of inventing a third
house style.

| Suite           | Skills used on this project                                                                                                         | Output location                  |
| --------------- | ----------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| **gstack**      | `/office-hours` (socratic design), `/plan-eng-review`, `/plan-ceo-review`, `/plan-design-review`, `/plan-devex-review`, `/autoplan` | `docs/initiatives/{initiative}/` |
| **superpowers** | `writing-plans`, `executing-plans`, `test-driven-development`, `requesting-code-review`, `finishing-a-development-branch`           | `docs/specs/{initiative}/`       |

Anything prefixed `gsd-*` belongs to a third suite (GSD) that this project explicitly does **not**
use. Ignore it if you see it referenced anywhere.

**What produced what, so far:**

- `docs/initiatives/todo-experience/DESIGN.md` — `/office-hours`, gstack, Builder mode. Its
  structure (Problem Statement → What Makes This Cool → Constraints → Premises → Approaches
  Considered → Recommended Approach → Open Questions → Success Criteria → Distribution Plan →
  Next Steps → What I noticed about how you think) is that skill's template, not free-form prose.
  **Preserve those section headings** when you edit it.
- The adversarial review in Task 1 is that same skill's **Spec Review Loop** step, which the
  originating session skipped for budget. The 5 dimensions, 3-iteration cap, and
  `## Reviewer Concerns` fallback in `REVIEW-PROMPT.txt` are its specification, not invented rules.

**The document contracts you must follow are in the repo, and you can read these:**

- `docs/templates/SPEC.md` — the shape `superpowers:writing-plans` writes into. Use it if you
  reach Task 3.
- `docs/templates/FEATURE.md` — post-implementation feature summaries. One per Spec, not per
  DevTask.
- `AGENTS.md` → File & Status Discipline — the lifecycle rules for all of the above.

**Two conventions to carry forward:**

1. `AGENTS.md` requires every planning document to name the skill that wrote it. `DESIGN.md`
   carries `Generated by /office-hours (gstack) on 2026-08-12`. Keep that line accurate.
2. **Attribute honestly.** If you perform work that a skill would normally do, say so plainly —
   e.g. `Reviewed by Codex following the /office-hours Spec Review Loop contract`. Do **not**
   write `Generated by /plan-eng-review` for a review you performed yourself. A false attribution
   makes the document trail useless for the next agent.

`AGENTS.md` → Skill Transition Discipline also requires stopping at every workflow boundary for
operator approval rather than chaining steps, and committing each workflow's artifacts as one
atomic commit. Both apply to you.

## Where things stand

- Branch `feat/plan-psykl-loop`, **two commits ahead of `main`, unpushed**:
  - `c0d8759` — roadmap retagged: milestones past M1/M2 use purpose tags instead of ordinal
    numbers; Apple-native and multi-tenant-auth deferred as unsequenced; a `psykl-loop`
    milestone opened as next.
  - `fdc55e4` — an `/office-hours` design session that **superseded** part of `c0d8759`: the
    operator wants Apple Reminders-grade todo experience first, and the PSYKL loop after.
- `main` is clean at `62e4105`, tagged `v0.3.0`. M1 and M2 are shipped.
- Working tree is clean. A husky + lint-staged pre-commit hook runs prettier, then
  `format:check` and `typecheck` across the workspace. Expect commits to take ~30s.

## Hard rules — violating any of these is a failure

From `AGENTS.md`, restated so you cannot miss them:

- **Never commit directly to `main`. Never force-push to `main`.**
- **Never merge any pull request without the operator's explicit approval in the current session.**
  This includes `gh pr merge`. No exceptions.
- **Never run bare `git push` on this branch.** It has no upstream. Use an explicit refspec:
  `git push -u origin HEAD:feat/plan-psykl-loop`. And only after asking.
- **Test-Driven Development is mandatory** for any code change: failing test → implementation →
  green → refactor. Tests ship in the same pull request as the behavior they cover.
- **Five-layer test pyramid** — static analysis, unit, integration, component, end-to-end. See
  `AGENTS.md` → Test Discipline for file locations and naming.
- **No milestone tokens in code filenames.** `m1`, `m2`, `psykl-loop` etc. belong in `docs/` only.
- Conventional Commits. Commit message headers describe the feature work only; documentation and
  status updates go in body bullets.

## Settled decisions — do not re-litigate these

The operator decided these in conversation. Treat them as locked. If you think one is wrong,
say so in one sentence and proceed anyway; do not silently redesign around them.

1. **Todo experience first, PSYKL loop second.** Not negotiable, not a sequencing suggestion.
2. **Dates:** a task may have a due date, and a time when the deadline genuinely has one.
   **No notifications, ever. No alerts. No repeat rules.** Recurrence arrives much later as
   template-tasks-that-spawn-instances, which is a different mechanism from Apple's repeat rules.
3. **Subtasks are deferred** until lists and sections have been used in real life. This is a
   deliberate validation gate, not an oversight.
4. **Single-user, multi-device, siloed. No collaboration, ever.** No sharing, no assignees, no
   comments. This kills a large fraction of Apple Reminders' feature set by definition.
5. **Approach B — structure first.** The sequence is: generalize the sync queue + Lists →
   Sections → manual ordering (fractional positions) → notes/priority/due date →
   swipes/density/animation → tags → search. You may argue about ordering _within_ B. Do not
   re-argue B versus A versus C.
6. **Fractional positions, not integer indexes,** for manual reorder. Decided before the feature
   exists specifically to avoid a migration over live data later.
7. **One feature per pull request**, deployed and lived with before the next starts.

## Your task queue, in order

### Task 1 — Adversarial review of the design doc

Run the review specified in `docs/initiatives/todo-experience/REVIEW-PROMPT.txt`. Ideally in a
sub-agent or fresh context so the reviewer has not seen this prompt; if that is not available,
do it yourself but read `DESIGN.md` cold and adversarially.

The most valuable thing you can find is a **false claim about the code**. The design doc asserts
that `components/web_client/src/db/idb.types.ts:9` couples the sync queue to tasks, and that
manual reorder conflicts destructively under Last-Write-Wins. Read
`components/web_client/src/sync/` and verify both rather than trusting them.

Fix each issue in `DESIGN.md`. Re-review. **Maximum 3 iterations.** If the same issues survive two
consecutive rounds, stop looping and persist them as a `## Reviewer Concerns` section in the doc.

Report to the operator: iterations, issues found, issues fixed, remaining concerns, quality score
out of 10. **Then stop and ask before marking the doc APPROVED.** Do not approve it yourself.

### Task 2 — Milestone close-out (only after the operator approves the doc)

The roadmap currently contradicts the design doc. This is known and was left unresolved on
purpose. Fix it:

1. `docs/initiatives/psykl-loop/MILESTONE.md` currently describes the loop work as the _active_
   milestone. Write `docs/initiatives/todo-experience/MILESTONE.md` from the approved design doc,
   and rewrite `psykl-loop/MILESTONE.md` as the _next_ milestone, seeded from the design doc's
   **Appendix: PSYKL loop specification**. Note: `git mv psykl-loop todo-experience` will fail —
   the target directory already exists.
2. Refresh the roadmap tables in `docs/PRODUCT.md` and `docs/PROJECT_STATUS.md` — both name
   `psykl-loop` as active. `todo-experience` becomes active; `psykl-loop` becomes next.
3. `docs/PROJECT_STATUS.md` also carries the active-initiative header block at the top. Update it.
4. Delete `HANDOFF.md`, `REVIEW-PROMPT.txt`, and this file — they are session artifacts, marked
   `deleted_at_spec_closeout: true`.
5. Verify: `grep -rn "psykl-loop" docs/ README.md` should only return intentional
   next-milestone references. Run `npx prettier --check "docs/**/*.md" "*.md"` — note that
   `docs/BACKLOG_IDEAS.md`, two feature docs, and the templates were already unformatted before
   this work; leave them alone.
6. Commit as one atomic docs commit.

### Task 3 — Only if the operator asks

Break the Approach B sequence into Specs and DevTasks per `AGENTS.md` → Spec/DevTask branching
workflow. Do not start this unprompted, and do not write any implementation code until a spec
exists and the operator has approved it.

## What not to do

- Do not write implementation code. Nothing in the queue above needs it. This is planning work.
- Do not push, open a pull request, or merge anything without asking first.
- Do not "helpfully" resolve the roadmap inconsistency before the design doc is approved — that
  ordering is deliberate.
- Do not rename shipped milestones M1/M2. Those tokens are load-bearing in 50 Architecture
  Decision Record IDs in `docs/ARCHITECTURE.md`, 12 feature-doc filenames, and `CHANGELOG.md`.

## Handback

When you stop, leave the operator: branch state, what you changed, what you verified and how,
anything you disagreed with, and the next command to run.
